# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @shop-saa-s/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t shop-saa-s-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system shop-saa-s-auth-service && \
          adduser --system -G shop-saa-s-auth-service shop-saa-s-auth-service

COPY apps/auth-service/dist shop-saa-s-auth-service/
COPY apps/auth-service/package.json shop-saa-s-auth-service/
RUN chown -R shop-saa-s-auth-service:shop-saa-s-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix shop-saa-s-auth-service --omit=dev -f install

CMD [ "node", "shop-saa-s-auth-service" ]
